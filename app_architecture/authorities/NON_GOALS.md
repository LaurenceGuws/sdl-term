# SDL Term Non-Goals Authority

- Owning cross-host shared runtime behavior.
- Owning cross-host shared rendering behavior.
- Introducing backend semantic changes for host convenience.
- Platform abstraction layers for hosts that do not yet exist.
- Compatibility/fallback/workaround shim paths.
